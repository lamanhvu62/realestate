specVersion: "v1beta"
serviceId: "realestate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "realestate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
